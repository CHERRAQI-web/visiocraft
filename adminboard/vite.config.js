export default {
  server: {
    port: 5174,  // Assurez-vous que le port reste fixe
    strictPort: true,  // Cela forcera Vite à utiliser uniquement ce port. S'il est déjà occupé, il échouera.
  }
}
